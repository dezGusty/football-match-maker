# Runtime environment
DOTNET_RUNNING_IN_CONTAINER=true
ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=http://+:5145
NODE_ENV=production

# Database
ConnectionStrings__DefaultConnection=Data Source=/app/data/footballdb.sqlite

# API Configuration
API_URL=http://backend:5145

# Security
JWT__Secret=JwtSecret

# Email Configuration
SMTP__Host=smtp.gmail.com
SMTP__Port=587
SMTP__User=mgmtsimulator4@gmail.com
SMTP__Pass=rgfrwwyk ruzymkeo
SMTP__EnableSsl=true

# Docker configuration
TAG=latest
UID=1000
GID=1000

# Resource limits (adjust based on your VPS resources)
DOTNET_MaxRAMPercent=80
NGINX_WORKER_PROCESSES=auto
NGINX_WORKER_CONNECTIONS=1024